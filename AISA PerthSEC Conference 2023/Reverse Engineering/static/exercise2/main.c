#include <stdio.h>
#include <string.h>

void decrypt(char* s, /* FIX ME*/) {
	for (int i = 0; /*FIX ME!! */; i ++) {
		s[i] -= key;
	}
}


int main(void) {
	char plaintext[] = "RZbRe]Y\\Va";
	
	decrypt(plaintext,13);
	//FIX ME!!
}
